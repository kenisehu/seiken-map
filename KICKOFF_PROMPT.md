# Claude Code 引き継ぎプロンプト（最初に貼る起動文）

---

新しいWebアプリを作ります。大腸がん検診で便潜血が陽性だった人が、精密検査（大腸内視鏡）を受けられる近隣の医療機関を探せるアプリ「精検実施機関マップ」です。同じディレクトリにある `HANDOFF.md` に詳しい仕様があるので、**まず必ず最初に `HANDOFF.md` を全部読んでから**着手してください。

## 進め方のお願い

僕（開発者）のスタイル：要件定義と実テストは僕がやる、設計と実装は君に任せる。説明は短めで、爆速で手を動かしてほしい。技術スタックは既存プロジェクトと統一で **React + Vite + TypeScript / Supabase / Vercel無料枠**。地図ライブラリは使わず、リスト主体＋Googleマップリンクでいく。ローカルは `npm run dev`、デプロイは `git push`。

## 今日のゴール（この順で）

1. `HANDOFF.md` を読んで、不明点があれば**最初にまとめて質問**して。なければ「仕様OK」と言って次へ。
2. プロジェクトの雛形を作成（Vite+React+TS、Supabaseクライアント、ディレクトリ構成）。
3. Supabaseのスキーマ定義（`facilities` / `reviews` / `events` の3テーブル。SQLマイグレーションを書く）。仕様書のスキーマに従う。
4. **ダミーデータ数件**で UI を先に完成させる（実データ収集はその後）。
   - リスト＋フィルター（コア5項目は常時表示、残りは詳細フィルターで開閉）
   - 住所/駅名検索、現在地から近い順ソート
   - 施設詳細ページ（電話テンプレ・バッジ・レビュー集計・フォーム補足欄）
   - 計測イベント（QRアクセス・電話タップ）の記録処理
   - レビュー投稿（選択式のみ・自由記述なし・承認制）
   - 免責フッター
5. UIが固まったら、**4市（那須市・所沢市・久喜市・武蔵野市）の実在データ収集**に進む。
   - 各自治体の「がん検診精密検査実施医療機関リスト」を一次ソースに、MCP/Chrome等で収集。
   - 各施設の公式サイトも見て、分かる範囲でフィルター属性を埋める。
   - 収集結果は `data/facilities.csv`（Supabaseのカラム名と一致するヘッダ）に整形。最終チェックは僕がやる。
   - フォーム回収用に `data/import_template.csv` も用意。

## 重要な制約（守ってほしい）

- レビューは**承認制・選択式のみ・自由記述カラムを作らない**（医療広告ガイドライン/誹謗中傷対策）。
- データは**オール実在**。憶測で施設情報を埋めない。分からない属性は空（null）にして、出典URL（`source_url`）を必ず残す。
- 患者側ログインは作らない。お気に入りも不要。
- スマホ優先のレスポンシブ。日本語のみ。
- 免責表示を常時フッターに出す（文言は仕様書参照）。

## 最初のアクション

`HANDOFF.md` を読んだら、(a) 仕様で曖昧な点の質問、または (b) 「仕様OK、雛形作成から始めます」のどちらかを返してください。その後、雛形作成 → スキーマ → ダミーデータでUI、と進めましょう。
